# Neural Net C++

A simple implementation of a fully connected neural network in C++ from scratch. No external heavy learning libraries, just standard library and math.

## Features
- Feedforward propagation
- Backpropagation (Gradient Descent)
- Sigmoid activation function
- Customizable layers

## Usage
```bash
g++ main.cpp -o neuralnet
./neuralnet
```
